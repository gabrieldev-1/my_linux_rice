#!/usr/bin/env bash

# Terminar instâncias já em execução
killall -q polybar

# Esperar até que os processos tenham sido encerrados
while pgrep -u $UID -x polybar >/dev/null; do sleep 1; done

# Lançar a barra principal
polybar mybar -c ~/.config/polybar/config.ini
